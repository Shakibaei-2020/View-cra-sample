import { ProjectCollaborator } from './project-collaborator';

describe('ProjectCollaborator', () => {
  it('should create an instance', () => {
    expect(new ProjectCollaborator()).toBeTruthy();
  });
});
