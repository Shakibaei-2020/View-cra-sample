import { CollaboratorService } from './collaborator-service';

describe('CollaboratorService', () => {
  it('should create an instance', () => {
    expect(new CollaboratorService()).toBeTruthy();
  });
});
