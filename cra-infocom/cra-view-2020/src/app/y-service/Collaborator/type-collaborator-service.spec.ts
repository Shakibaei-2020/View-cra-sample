import { TypeCollaboratorService } from './type-collaborator-service';

describe('TypeCollaboratorService', () => {
  it('should create an instance', () => {
    expect(new TypeCollaboratorService()).toBeTruthy();
  });
});
