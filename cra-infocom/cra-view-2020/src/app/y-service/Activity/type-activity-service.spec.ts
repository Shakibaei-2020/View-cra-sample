import { TypeActivityService } from './type-activity-service';

describe('TypeActivityService', () => {
  it('should create an instance', () => {
    expect(new TypeActivityService()).toBeTruthy();
  });
});
