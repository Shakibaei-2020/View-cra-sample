import { TypeExpenseService } from './type-expense-service';

describe('TypeExpenseService', () => {
  it('should create an instance', () => {
    expect(new TypeExpenseService()).toBeTruthy();
  });
});
