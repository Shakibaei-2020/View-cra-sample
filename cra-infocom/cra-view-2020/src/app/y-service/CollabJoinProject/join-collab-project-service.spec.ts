import { JoinCollabProjectService } from './join-collab-project-service';

describe('JoinCollabProjectService', () => {
  it('should create an instance', () => {
    expect(new JoinCollabProjectService()).toBeTruthy();
  });
});
