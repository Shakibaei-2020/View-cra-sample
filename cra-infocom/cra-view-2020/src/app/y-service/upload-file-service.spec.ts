import { UploadFileService } from './upload-file-service';

describe('UploadFileService', () => {
  it('should create an instance', () => {
    expect(new UploadFileService()).toBeTruthy();
  });
});
